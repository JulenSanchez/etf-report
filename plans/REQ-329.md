# REQ-329: Zen 派前沿升级 — Sortino 优化 + 前端对齐

**优先级**: 🟡 P1 | **目标版本**: v3.10.0 | **依赖**: pool 引擎就绪 | **状态**: 规划中

## 背景

Zen 派无风险轴，优化目标是 max Sortino。当前 `iterative_optimizer.py --school zen` 已原生支持（`target_metric=6y_sortino`, `prune=top-20`）。

需完成：首轮优化填池 → 产出 frontier → 前端消费。

## 方案

```
优化: python scripts/iterative_optimizer.py --school zen
      → research/params/zen/pool.json
      → build_frontier_output(school='zen')
      → research/params/frontier_zen.json

前端: type="single" — 展示最优 Sortino 点 + 参数卡片
```

## 改动范围

| 文件 | 改动 |
|------|------|
| `templates/tuner.html` | Zen 区域 type="single" 渲染 (从 frontier_zen.json 读取) |
| `scripts/quant_tuner.py` | `/api/frontier` 的 zen 分支返回有效数据 (已就绪) |

## 验收

1. Zen 优化产出 >= 1 个 Sortino 优于 zen-1 的参数组
2. 前端展示最佳 Zen 参数（非滑块，卡片式）
3. `/api/frontier` 的 zen 分支返回有效数据
