# REQ-282: 权重转换逻辑工程化修复

**优先级**: 🟠 High  
**估时**: 2-3h  
**状态**: backlog  
**目标版本**: v3.7.0  
**依赖**: REQ-281  
**创建日期**: 2026-06-13

---

## 动机

REQ-281 中为了绕过 `int(weight * 100)` 的浮点截断问题，将 `preset1.scoring.weights.ema_deviation` 写成 `0.5700000000000001`。这能运行，但配置可读性差，后续新增 preset 时还会踩坑。

## 实施范围

- 将 `scripts/quant_contract.py:preset_to_tuner_params()` 的权重转换从 `int(x * 100)` 改为稳定舍入逻辑。
- 将 `config/quant_universe.yaml` 中 `0.5700000000000001` 恢复为 `0.57`。
- 补充覆盖 `0.57/0.17/0.26 -> 57/17/26` 的测试。
- 验证 Tuner `/api/presets`、`quant_build_payload.py` 与回测一致性。

## 验证方式

| 验证项 | 方法 | 通过标准 |
|---|---|---|
| 权重转换 | 单测 | preset1 权重转为 57/17/26，总和 100 |
| payload | `python scripts/quant_build_payload.py` | 无权重和 warning |
| 回归 | `pytest tests -q` | 全绿 |
