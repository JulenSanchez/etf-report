# REQ-362: 清理旧前沿自动管线 + 提取优化原子

**优先级**: 🔴 P0 | **状态**: ✅ 已交付 | **目标版本**: v3.13.0

## 背景

2026-07-14 事实对齐结论：

1. Tuner 前沿已迁移到 `config/preset_metrics.json`（用户精调），不再依赖 `frontier_*.json`
2. 15 预设模式保留（gam/zen/act 各 5 档），不再做流派删除
3. 旧自动管线（pool.json → iterative_optimizer → build_frontier_output → frontier_*.json）已断链，需清理
4. 管线中的原子化优化基建（ParamSpace / BacktestRunner / Optuna objective / 指标提取）提取到 `research_utils.py`，保留复用

## 范围

### 一、删除（7 文件）

| 文件 | 原因 |
|------|------|
| `scripts/quant_optimizer.py` | 已 DEPRECATED，原子提取后删除 |
| `scripts/iterative_optimizer.py` | 旧管线入口 |
| `scripts/pareto_optimizer.py` | 旧冷启动优化器 |
| `scripts/run_optimization.py` | 旧管线启动脚本 |
| `scripts/optimization_analyzer.py` | Optuna 耦合，旧管线删除后输入源断 |
| `research/params/gambler/pool.json` | 旧管线 trial 缓存 |
| `research/params/zen/pool.json` | 旧管线 trial 缓存 |
| `research/params/actuary/pool.json` | 旧管线 trial 缓存 |

### 二、quant_contract.py — 删除死代码

| 删除 | 原因 |
|------|------|
| `build_frontier_output()` | 输出 `frontier_*.json`，Tuner 不再消费 |
| `compute_frontier()` | 仅被 build_frontier_output / pareto_optimizer 使用 |
| `seed_params_from_presets()` | 仅 iterative_optimizer 使用 |
| `INITIAL_PRESETS` | 优化器 seed 默认值 |
| `PRESET_OPT_PROFILES` | 优化器配置 |
| `OPT_PRESETS` | 优化器 preset 列表 |
| `mark_preset_unreliable()` | 优化器辅助 |
| `PARAM_BOUNDS` | 仅旧优化器使用 |
| `get_param_bounds()` | 仅旧优化器使用 |
| `get_param_type()` | 仅旧优化器使用 |
| `auto_bounds()` | 仅旧优化器使用 |
| `preset_to_tuner_params()` | 仅旧优化器 + optimization_analyzer 使用 |
| `_WEIGHT_PARAM_KEYS` | 仅旧优化器使用 |

### 三、提取原子 → `scripts/research_utils.py`

| 来源 | 原子 | 说明 |
|------|------|------|
| `quant_optimizer.py` | `ParamSpace` | 参数空间：bounds → grid/random 采样 |
| `quant_optimizer.py` | `BacktestRunner` | 带预加载的回测执行器 |
| `quant_optimizer.py` | `_run_trial_optuna()` → `optuna_objective()` | Optuna TPE trial objective |
| `quant_optimizer.py` | `_extract_metrics()` | 从 NAV 提取指标字典 |

### 四、tuner.html

- 删除键盘快捷键 1（zen-1）/ 2（act-1）绑定

### 五、文档

- `docs/runbook/v2-quant/optimization.md` — 去掉旧管线章节
- `plans/Board.md` — 更新状态

### 不动

- 历史 `plans/REQ-*.md`、`research/params/*/report.md`
- `research/params/gambler/` 下的报告、`research/params/*/archive/`
- `research/params/runs/`、`research/params/preset_warm.json`、`seed_params.json`
- `config/preset_metrics.json`（当前前沿事实源）
- `quant_contract.py` 中 Tuner 仍使用的 3 个符号：`DEFAULT_PRESET` `validate_tuner_params` `tuner_params_to_config_override`

## 验证

1. `pytest tests/ -x -q` — 全绿（确认删除的文件不在 test import 链中）
2. `python scripts/quant_tuner.py` — Tuner 正常启动，`/api/frontier` `/api/presets` `/api/save` 正常
3. `python -c "from research_utils import *"` — 新原子可用
4. `python scripts/quant_backtest.py --preset gam-0 --start 2025-01-01 --end 2025-06-01` — CLI 回测正常
