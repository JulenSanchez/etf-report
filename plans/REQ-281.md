# REQ-281: etf-report 项目仓库化迁移与 stable 发布链收口

**优先级**: 🟠 High  
**估时**: 已完成一期  
**状态**: done  
**目标版本**: v3.7.0 / v4.0.0 过渡  
**依赖**: 无  
**创建日期**: 2026-06-13  
**完成日期**: 2026-06-13

---

## 动机

`etf-report` 已从单一 AI Skill 演化为完整项目：正式页报告、量化回测、Tuner、本地定时任务、GitHub Pages 发布、研究归档和需求治理同时存在。继续放在 `.claude/skills/` 下会造成身份混乱、文档过载、发布链风险和路径耦合。

本需求记录 2026-06-13 已完成的一期项目仓库化迁移。后续优化不继续塞在本需求下，而是拆为独立需求进入看板。

---

## 完成记录

### 1. 评审与迁移方案

- 评审 `C:/Users/julentan/StockMarket/.claude/skills/etf-report`。
- 输出桌面评审报告：`C:/Users/julentan/Desktop/完整评审报告.md`。
- 明确三条方向：去 Skill 化、v1/v2-v3 产品线拆分、结构精简。
- 增补 GitHub / stable 仓 / 本地迁址风险方案。

### 2. 主项目迁出 `.claude/skills`

- 新主项目位置确认：`C:/Users/julentan/etf-report`。
- Git 根目录确认：`git rev-parse --show-toplevel` 指向新目录。
- 远端保留：`https://github.com/JulenSanchez/etf-report.git`。
- 旧目录未自动删除：因占用导致 move 失败，用户后续手动删除。

### 3. 去 Skill 化与项目文档重组

- `SKILL.md` 降级为旧 Skill 兼容入口。
- 新增 `docs/ai/AGENT_GUIDE.md`。
- `statusbar.config.md` / `rules/` 移入 `docs/ai/legacy/`。
- `README.md` 重写为普通项目 README。
- `DESIGN.md` / `design/` / `runbooks/` 迁入 `docs/design/` 与 `docs/runbook/`。
- 修复当前文档入口中的旧 `WORKFLOW.md` / `QUANT_SYSTEM.md` / `runbooks/*` 引用。

### 4. report-site 与 quant-lab 边界拆分

- 新增 `scripts/report_site/` 兼容入口。
- 新增 `scripts/quant_lab/` 兼容入口。
- 保留旧 `scripts/*.py` 入口，避免日常命令断裂。
- 新增统一路径模块 `src/etf_report/core/paths.py`。
- 消除 `scripts/` 下 `SKILL_DIR / skill_dir / 技能根目录` 语义。

### 5. GitHub Pages 发布安全修正

- 保留根目录 `index.html` / `assets/` 作为当前 GitHub Pages 发布面。
- `scripts/deployer.py` 去除默认 `--force-with-lease`。
- `BatchFiles/Release.bat` 去除 force push。
- 远端更新使用普通 `git push origin main`。
- 遇到远端领先时执行 `fetch + rebase`，未强推。

### 6. 量化权重和 payload 修复

- 修复四舍五入导致的权重和问题：`preset1.scoring.weights.ema_deviation: 0.5700000000000001`。
- 当前 `quant_contract.py` 的 `int(weight*100)` 转换下所有 preset 权重和均为 100。
- `python scripts/quant_build_payload.py` 成功生成 payload。

### 7. 健康检查收口

- 移除解释层鲜度检查 `WorkflowChecker.check_editorial_freshness`。
- 删除对应测试。
- `python scripts/health_check.py` 达到 `25/25 PASS`。
- `python scripts/verify_html_integrity.py` 达到 `16 PASS / 0 FAIL`。
- `pytest tests -q` 达到 `211 passed`。

### 8. stable 仓库与计划任务收口

- 更新稳定仓：`C:/Users/julentan/etf-report-stable`。
- 重新注册 Windows 计划任务：`etf早盘报告`、`etf午盘报告`、`etf盘后数据更新`、`etf报告发布`。
- 修复 `BatchFiles/setup_quant_tasks.ps1` / `setup_report_task.ps1` 中 `repoDir` 多取一级父目录的问题。
- stable 本地 `preclose_push.py` 的 Top10 扩展为 Top12 并提交。
- stable 更新前 stash 已检查并删除：旧 `editorial_content.yaml` 与重复 `git_pull.bat` 不再保留。

### 9. 远端提交记录

- `ef430e6 refactor: convert etf-report to project layout`
- `68f3e06 test: remove editorial freshness health check`
- `9840c91 fix: correct scheduled task setup paths`
- `a826af0 feat: expand preclose push list to top12`
- `c4d57fe fix: keep project root path in preclose push`

---

## 派生需求

后续优化拆为独立需求，不继续在 REQ-281 下挂子任务：

| ID | 标题 | 说明 |
|---|---|---|
| REQ-282 | 权重转换逻辑工程化修复 | 消除 `0.5700000000000001` 这类浮点绕法 |
| REQ-283 | 量化运维文档拆分精简 | 将 `docs/runbook/v2-quant/overview.md` 拆成短文档 |
| REQ-284 | shared 模块正式包化 | 将 shared 脚本迁入 `src/etf_report/core/` |
| REQ-285 | GitHub Pages 发布面二期迁移 | 评估 `web/` / `dist/` / Actions Pages |
| REQ-279 | data 目录结构优化 | 已有需求，承接运行产物治理 |
| REQ-286 | PLAN 与历史治理文档去 Skill 化 | 清理 `PLAN.md` 与历史治理旧术语 |
| REQ-287 | stable 自动更新与计划任务观测 | 明确 stable 更新策略和计划任务日志 |
| REQ-288 | 双仓拆分评估 | 评估 report-site / quant-lab 是否拆仓 |

---

## 文件影响分析

| 文件/目录 | 改动类型 | 说明 |
|---|---|---|
| `SKILL.md` | 重写 | 降级为旧 Skill 兼容入口 |
| `README.md` | 重写 | 普通项目仓库说明 |
| `docs/ai/` | 新增/迁移 | AI 协作指南与 legacy 规则 |
| `docs/runbook/` | 新增/迁移 | 运维、发布、审计文档 |
| `docs/design/` | 新增/迁移 | 架构与设计文档 |
| `src/etf_report/core/paths.py` | 新增 | 统一项目路径解析 |
| `scripts/report_site/` | 新增 | v1 正式页兼容入口 |
| `scripts/quant_lab/` | 新增 | v2/v3 量化兼容入口 |
| `scripts/deployer.py` | 修改 | 去除默认 force push，改项目仓语义 |
| `scripts/health_check.py` | 修改 | 去除解释层鲜度检查，更新备份检查 |
| `scripts/preclose_push.py` | 修改 | Top12 推送，保留 PROJECT_ROOT 路径 |
| `BatchFiles/setup_*.ps1` | 修改 | 修复计划任务 repoDir 路径 |
| `config/quant_universe.yaml` | 修改 | 临时修正 preset1 权重浮点截断 |

---

## 验证记录

| 验证项 | 结果 |
|---|---|
| `pytest tests -q` | `211 passed` |
| `python -m compileall ...` | PASS |
| `python scripts/quant_build_payload.py` | PASS |
| `python scripts/verify_html_integrity.py` | `16 PASS / 0 FAIL` |
| `python scripts/health_check.py` | `25/25 PASS` |
| Windows 计划任务 | 4 个任务 Ready，均指向 `etf-report-stable` |

---

## 评审记录

- 2026-06-13: 完成项目仓库化一期迁移，远端推送完成，stable 仓与计划任务同步完成。
- 2026-06-13: [用户确认] stable 更新前 stash 中旧 `editorial_content.yaml` 与重复 `git_pull.bat` 不需要保留，已删除 stash。
- 2026-06-13: [用户决定] 解释层鲜度检查直接删除，不再作为健康检查项。
- 2026-06-13: [用户决定] `preclose_push.py` 推送列表从 Top10 扩到 Top12，并从 stable 提交。
- 2026-06-13: [用户覆盖] AI 初始记录方式：在 REQ-281 下挂多个后续子任务；用户提示：若看板更适合拆多个需求，则不要额外加子任务概念。最终调整：REQ-281 只记录已完成的一期迁移，后续优化拆为 REQ-282~REQ-288 与既有 REQ-279。
