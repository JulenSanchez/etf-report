# REQ-284: shared 模块正式包化

**优先级**: 🟡 Normal  
**估时**: 6-10h  
**状态**: in_progress  
**目标版本**: v4.0.0  
**依赖**: REQ-281  
**创建日期**: 2026-06-13

---

## 动机

REQ-281 新增了 `src/etf_report/core/paths.py`，但大多数 shared 模块仍位于 `scripts/`，测试仍大量通过 `sys.path` 注入脚本目录。项目已经仓库化，下一步应逐步转为正常 Python 包结构。

## 实施范围

- 将 shared 模块逐步迁入 `src/etf_report/core/`：`config_manager.py`、`logger.py`、`trading_calendar.py` 等。
- 将 report-site 与 quant-lab 的业务模块分别迁入包目录。
- 保留 `scripts/*.py` 作为 CLI wrapper。
- 更新测试 import，不再直接依赖 `scripts/` 注入。

## 进展记录

- 2026-06-16: Phase 1 完成首批 shared 模块迁移：`config_manager.py`、`logger.py`、`trading_calendar.py` 已复制到 `src/etf_report/core/`，`scripts/` 下保留兼容 wrapper。
- 2026-06-16: `tests/conftest.py` 已支持 wrapper 后重置 config manager 实现模块状态。
- 2026-06-16: `python -m pytest tests/test_config.py tests/test_realtime.py tests/test_health_check.py -q` 通过，30 passed。
- 后续仍需继续迁移更多 shared 模块；当前避免触碰 `quant_optimizer.py` 等用户 WIP。

## 验证方式

| 验证项 | 方法 | 通过标准 |
|---|---|---|
| 导入链 | `pytest tests -q` | 全绿 |
| CLI 兼容 | 运行旧 `scripts/*.py` 入口 | 行为不变 |
| 新包入口 | 运行新包内模块或 wrapper | 行为一致 |
