# REQ-118: HTML 结构清理：语义 class + grid-template-areas

**优先级**: 🟡 Medium（历史回填，近似）
**估时**: 2-4h（历史回填，近似）
**状态**: done

**目标版本**: v2.3.0
**依赖**: 无
**创建日期**: -
**最后活动**: 2026-04-13

---

## 动机

这是一次对 `index.html` 结构层的历史整理需求，目标是减少脆弱选择器和杂乱样式，让页面结构更稳定、更易维护。

## 已知事实

当前只能从已有锚点回填出以下确定信息：

- `Board.md` 记录标题为“HTML 结构清理：语义 class + grid-template-areas”
- `Board.md` 记录完成结果：**3 步全完成；内联样式 152→4；6 面板 24 section 加语义 class；`nth-of-type` 全部替换为 `grid-template-areas`；14 项验证全 PASS**
- `Archive.md` 记录该需求被归入 **`v2.3.0`** 发布

## 最小成果摘要

- 大幅收敛内联样式数量
- 为主要面板 / section 补齐语义 class
- 去除依赖结构顺序的 `nth-of-type` 布局写法
- 改为使用 `grid-template-areas` 承接布局语义
- 完成一轮通过的结构校验

## 文件影响分析

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `index.html` | 修改 | 清理结构、补语义 class、替换布局定位方式 |
| 相关校验脚本 | 修改 | 配合新的 HTML 结构完成验证 |

## 验证方式

| 验证项 | 方法 | 结果 |
|--------|------|------|
| HTML 结构清理 | 历史记录回填 | 已完成 |
| 校验通过情况 | 看板锚点 | 14 项验证全 PASS |

## 评审记录

- 2026-04-13: 历史锚点显示本需求已完成，并进入 `Board.md` 的 done 区。
- 2026-04-17: 补建缺失的 `REQ-118.md`；本单按最低成本回填，仅保留归档所需的最小信息。
- 2026-04-17: 发现历史锚点存在描述差异：`Board.md` 将本单记为 HTML 结构治理，`Archive.md` 在 `v2.3.0` 备注中写为“状态栏栈模型 + 人格切换 + 版本发布一条龙”；当前不追溯修正，仅原样保留供后续需要时参考。
